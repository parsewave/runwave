[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "runwavepy"
version = "0.1.0"
description = "Python wrapper around the runwave Node CLI"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["runwavepy"]
