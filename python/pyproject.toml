[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "runwave"
version = "0.1.0"
description = "Python wrapper around the runwave-playtest Node CLI"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["runwave"]
